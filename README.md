# @audio/vocals

> Center-channel vocal isolation / removal. All planned.

A tested implementation exists in the `audio` package (`fn/vocals.js` — center isolate/remove, stream≡read verified) — extract at the audio-module migration, don't rewrite. ML stem separation is out of scope here (see `@audio/mir` deferred tier).
